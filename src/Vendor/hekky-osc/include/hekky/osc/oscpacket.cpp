#include "oscpacket.hpp"

namespace hekky {
    namespace osc {
        /*
        
        char* OscPacket::GetBytes(uint64_t& size)
        {
            size = 0;
            return nullptr;
        }
        
        */
    }
}
